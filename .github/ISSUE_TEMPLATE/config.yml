blank_issues_enabled: true
contact_links:
  - name: PyPI package (xaikit-py)
    url: https://pypi.org/project/xaikit-py/
    about: Install the published Beta 0.1.0 (`uv add xaikit-py` / `pip install xaikit-py`); the import is still `xaikit`.
