blank_issues_enabled: true
contact_links:
  - name: PyPI package (xaikit-py)
    url: https://pypi.org/project/xaikit-py/
    about: Install xaikit-py (`uv add xaikit-py` / `pip install xaikit-py`); the import is still `xaikit`. This repo's version is 0.1.1.
